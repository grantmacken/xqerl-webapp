worker_processes 1;
# Note: error log with debug used during development
error_log logs/error.log debug;
pcre_jit on;
# increase limit
worker_rlimit_nofile 8000;
include events.conf;
# include env.conf;
http {
    include http.conf;
    include mime.types;
    include redirect.conf;    # redirect port 80 to port 433
    include server.conf;      # it all happens here
    # internal network cmark API calls on port 85
    # include cmarkPort.conf;
}
